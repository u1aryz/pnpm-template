pre-commit:
  commands:
    check:
      run: pnpm biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files} && pnpm typecheck
      stage_fixed: true

commit-msg:
  commands:
    "lint commit message":
      run: pnpm commitlint --edit "$1"
